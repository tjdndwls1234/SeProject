<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.PlanMate.mapper.StudentReportMapper">
    <select id="retakeableCourseListSearch" resultType="String">
        SELECT COURSE_CODE
        FROM STUDENT_TABLE s_t
        WHERE s_t.STUDENT_CODE = #{studentCode}
            AND s_t.COURSE_REPORT = 'C+' AND s_t.COURSE_REPORT = 'C0' AND s_t.COURSE_REPORT = 'D+'
            AND s_t.COURSE_REPORT = 'D0' AND s_t.COURSE_REPORT = 'F'
    </select>
</mapper>